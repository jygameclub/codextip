# CodexTip 开发规则

## 沟通与范围

- 默认使用中文回答，先给结论，再分点说明；命令提供可复制代码块。
- 保持当前分支，不切换分支，不擅自重写提交历史。
- 修改前检查 Git 状态，保留用户和其他任务的改动，只提交当前任务涉及的文件。

## 完成任务后的提交与推送

- 每次完成涉及文件修改的任务，必须完成验证、创建 Git 提交并推送到当前分支对应的远程分支；默认远程为 `origin`。
- 使用清晰、具体的中文提交信息，说明实际完成的功能或修复。
- 提交前执行 `git diff --check`；Swift 逻辑变更执行 `swift test`，应用代码或资源变更执行 `./scripts/build.sh`。
- 推送后核对本地 HEAD 与远程分支的提交 SHA，不能仅凭本地提交成功就报告已完成推送。
- 不使用强制推送。遇到远程分歧、认证失败或无法安全处理的冲突时保留已完成的工作，说明具体原因，不隐瞒失败。
- 纯咨询、只读检查且没有文件修改时，不创建空提交。
- 用户在当前任务中的明确指令优先，例如要求暂不提交或暂不推送时遵从该要求。

## 产品与实现

- 保持原生 macOS 菜单栏应用形态，后台运行，不播放声音，不无故打开浏览器或前台窗口。
- 用户可见的文案、状态、设置和错误提示同时维护简体中文与英文，保持语言切换与已有设置兼容。
- 修改功能、安装方式或统计含义时，同步更新 `README.md` 与 `README.en.md`。
- 额度只使用 Codex App Server 的只读接口，不创建模型任务，不消耗重置券，不修改用户认证状态。
- 本地 token 历史可只读扫描 Codex 的 sessions / archived_sessions；只提取用量、时间、模型与去重所需元数据，不存储聊天正文。此统计按本机日志合并，不按登录账号过滤；与订阅额度历史分开存储。
- 费用功能使用可追溯、带核对日期的公开单价，明确标注标准 API 等值估算；缺少模型或计费类别价格时显示未计价用量，不猜测、不当成免费，不把估算当作订阅账单。
- 统计不得把缺失数据当成零；正确处理采样中断、时间变化、账户切换和额度重置。
- 不提交真实额度历史、账户标识、认证文件、凭据或本机构建目录；文档截图使用演示数据。
- 资源应说明来源；菜单栏图标需兼顾深浅色模式与可访问性，不能只靠颜色传达状态。

## 验证与交付

- UI 改动优先使用应用自带的离屏渲染验证，检查中文、英文及深浅色模式。
- 页签、滚动或弹窗布局改动同时运行离屏交互回归测试，验证反复切换与刷新后的弹窗尺寸和按钮命中区域，不能仅凭静态截图判断交互正常。
- 需要浏览器验证时，默认使用 Playwright 后台运行，不打开用户可见的 Google Chrome，不播放声音。
- 更新本机应用时保留历史和设置，确认新版本正在运行。
- 最终简要说明完成内容、验证结果、提交号及推送结果；未验证的内容明确说明。
